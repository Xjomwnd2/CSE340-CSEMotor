const express = require('express');
const router = express.Router();

// Define some example routes for motors
router.get('/', (req, res) => {
  res.render('motors', { title: 'Motors' });
});

// Export the router
module.exports = router;
